package com.example.junit

//calculator test
class Calculator {
    fun add(first: Int, second: Int): Int {     // Metode add menerima dua argumen bertipe data Int, first dan second.
        return first + second                   // Metode ini mengembalikan hasil penjumlahan dari first + second.
    }
}
